export default function isJson(jsonString) {
    try {
        const o = JSON.parse(jsonString);
        if (o && typeof o === 'object') {
            return o;
        }
    }
    catch {
        return null;
    }
    return null;
}
